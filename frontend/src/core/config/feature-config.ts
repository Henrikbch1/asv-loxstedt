/**
 * Defines which optional feature modules are enabled in this project.
 *
 * Toggle a feature here without touching layout, routing, or CMS code.
 * Modules listed here correspond to folders under `src/features/`.
 */
export const featureConfig = {
  /** Renders a calendar section on the home page. */
  calendar: {
    enabled: true,
  },
} as const;
