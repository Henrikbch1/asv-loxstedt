/**
 * defaults.ts – fallback values used when CMS data is missing or invalid.
 */
export const defaults = {
  siteName: 'Vereinswebsite',
  fallbackTemplate: 'default',
  homeSlug: 'home',
  pageTitle: 'Seite',
  newsTitle: 'News',
  boardTitle: 'Vorstand',
} as const;
